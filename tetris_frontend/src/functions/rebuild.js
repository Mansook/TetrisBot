import { CurrentBlock, NextBlock } from "../global";
import { drawBlock } from "./drawBlock";

export const rebuild = () => {
  // drawBlock(CurrentBlock, ctxMainBoard);
  //drawBlock(NextBlock, ctxNextBoard);
};
